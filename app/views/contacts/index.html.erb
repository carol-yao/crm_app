<div class="contact">
  <h1>All Contacts</h1>
    <ul class= "contact_listings">
    <% @contacts.each do |contact| %>
    <li><%= link_to "#{contact.first_name} #{contact.last_name}", contact_path(contact), class: "contact_links" %></li>
    <hr>
    <% end %>
    </ul>
</div>
